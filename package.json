{
  "name": "mtbank-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "cd final-web-main && npm run dev",
    "build": "cd final-web-main && npm install && npm run build",
    "preview": "cd final-web-main && npm run preview"
  }
}
